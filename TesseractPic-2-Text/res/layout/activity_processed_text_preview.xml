<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="top"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".ProcessedTextPreview" >

    <TextSwitcher
        android:id="@+id/textSwitcher1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true" >

    </TextSwitcher>

    <EditText
        android:id="@+id/text_preview"
        android:layout_width="fill_parent"
        android:layout_height="375dp"
        android:ems="10"
        android:inputType="textMultiLine" />

    <Button
        android:id="@+id/cancel_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_toRightOf="@+id/textSwitcher1"
        android:text="Cancel"
        android:textColor="#FFFFFF" />

    <Button
        android:id="@+id/copy_2_clipboard"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/cancel_button"
        android:layout_alignBottom="@+id/cancel_button"
        android:layout_alignRight="@+id/text_preview"
        android:text="Copy Text"
        android:textColor="#FFFFFF" />

</RelativeLayout>
